## What does this change, and why?

<!-- The diff already shows what changed. Explain why it was needed. -->

## Related issue

<!-- If this is a larger change, link the issue where it was discussed first (see CONTRIBUTING.md). Small, focused PRs don't need one. -->

## Testing

- [ ] `pytest` passes locally
- [ ] Added or updated tests for the behavior this PR changes
- [ ] If this touches a security-sensitive path (sandbox boundary, protected files, approval flow), I tested it against real behavior, not only mocks — see `tests/test_path_safety_escape.py` for the pattern this project prefers there

## Checklist

- [ ] This PR does one thing (unrelated cleanup noticed along the way is mentioned below instead of folded in)
- [ ] No unrelated files changed
